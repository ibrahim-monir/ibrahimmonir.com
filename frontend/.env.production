# Public API base used by the production build (Hostinger).
# NEXT_PUBLIC_* vars are inlined at build time, so this must be present when `next build` runs.
NEXT_PUBLIC_API_URL=https://api.ibrahimmonir.com/api
